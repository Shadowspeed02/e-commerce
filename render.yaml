services:
  - type: web
    name: ecommerce-website
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MODNO_DB
        sync: false
      - key: JWT
        sync: false
    healthCheckPath: /
